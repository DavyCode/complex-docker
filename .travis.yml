sudo: required
services: 
  - docker

before_install:
  - docker build -t davycode/react-test -f Dockerfile.dev ./client

script:
  - docker run davycode/react-test npm run test -- --coverage

after_success:
  - docker build -t registry.gitlab.com/davycode/complex-docker-client ./client
  - docker build -t registry.gitlab.com/davycode/complex-docker-api ./server
  - docker build -t registry.gitlab.com/davycode/complex-docker-worker ./worker
  - docker build -t registry.gitlab.com/davycode/complex-docker-nginx ./nginx
# setup env to docker image registry
  - echo "$REGISTRY_PASSWORD" | docker login -u "$REGISTRY_USER" --password-stdin
# push images to docker image registry
  - docker push registry.gitlab.com/davycode/complex-docker-client
  - docker push registry.gitlab.com/davycode/complex-docker-api
  - docker push registry.gitlab.com/davycode/complex-docker-worker
  - docker push registry.gitlab.com/davycode/complex-docker-nginx